[profile.default]
src = "src"
# P256Validator is compiled from the main contracts project via the `zbacs/` remapping.
out = "out"
test = "test"
libs = ["../../contracts/lib"]
allow_paths = ["../../contracts"]
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
# Fork tests read vectors/userop.json produced by scripts/kernel-account.mjs
fs_permissions = [{ access = "read", path = "./vectors" }]

[fmt]
line_length = 110
tab_width = 4
bracket_spacing = false
int_types = "long"

[rpc_endpoints]
base_sepolia = "${BASE_SEPOLIA_RPC_URL}"
