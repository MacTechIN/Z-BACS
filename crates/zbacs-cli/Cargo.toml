[package]
name = "zbacs-cli"
description = "Z-BACS PoC command line: keygen / seal / open / inspect"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "zbacs"
path = "src/main.rs"

[dependencies]
zbacs-core = { path = "../zbacs-core" }
# The forensic session (Z-1.Q.2) drives the same workspace and wipe the Agent uses.
zbacs-session = { path = "../zbacs-session" }
anyhow.workspace = true
clap.workspace = true
hex.workspace = true
serde.workspace = true
serde_json = "1"
